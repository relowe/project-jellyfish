definitions
    function fib(n: number) returns number
        if n <= 1 then
            return n
        end if
        return fib(n-1) + fib(n-2)
    end function
end definitions

program
    _a: text = "Hi"
    print("Hello\ nWorld!", _a)
end program