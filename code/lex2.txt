definitions
    function fib(n: number) returns number
        if n <= 1 then
            return n
        end if
        return fib(n-1) + fib(n-2)
    end function
end definitions

program
    print(fib(10))
end program